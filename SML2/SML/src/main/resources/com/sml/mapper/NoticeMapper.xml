<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.sml.mapper.NoticeMapper">
    
    <!-- 공지사항 글 등록 -->          <!--  파일업로드에서 notice_code를 가져오기 위해 사용 -->
    <insert id="noticeRegister" useGeneratedKeys="true" keyProperty="noticeCode">
    insert into NOTICE (MEM_CODE,CATEGORY_CODE, NOTICE_TITLE, NOTICE_BODY)
    values (#{memCode},#{categoryCode},#{noticeTitle},#{noticeBody})
    </insert>
   
   <!--파일 업로드-->
   <insert id="fileUp">
    INSERT INTO FILE_UP (NOTICE_CODE, FILE_NAME, FILE_PATH, FILE_TYPE)
    values (#{noticeCode},#{FileName},#{FilePath},#{FileType})
   </insert>
  </mapper>